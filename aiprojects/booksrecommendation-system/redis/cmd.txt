#set up compose folder
sudo docker-compose up -d

# cleanup
 sudo docker container stop redis-local
 sudo docker container rm redis-local

#run redis 
sudo docker run -d --name redis-local -p 6379:6379 -p 8001:8001 -v $(pwd):/data redis/redis-stack:latest

# connect  cli 
sudo docker exec -it redis-local redis-cli

docker cp redis-local:/data/dump.rdb .

sudo docker-compose down -v

# redis cmd


